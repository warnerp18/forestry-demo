(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define('radius/notification/js/index', ['exports'], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports);
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports);
    global.radiusNotificationJsIndex = mod.exports;
  }
})(this, function (exports) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });

  exports['default'] = function () {
    var el = document.querySelector('[data-js-notification-collapse]');
    var WAIT_TIME = 10000;
    var COLLAPSED_CLASS = 'is-collapsed';

    if (!el) {
      return;
    }

    setTimeout(function () {
      window.requestAnimationFrame(function () {
        el.style.height = el.clientHeight + 'px';

        window.requestAnimationFrame(function () {
          el.classList.add(COLLAPSED_CLASS);
        });
      });
    }, WAIT_TIME);
  };
});