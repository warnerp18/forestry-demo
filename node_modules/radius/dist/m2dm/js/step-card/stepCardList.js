(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define('radius/step-card/js/stepCardList', ['exports'], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports);
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports);
    global.radiusStepCardJsStepCardList = mod.exports;
  }
})(this, function (exports) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });

  exports['default'] = function () {
    var CLASS_INACTIVE = 'is-inactive';
    var cardLists = Array.from(document.querySelectorAll('[data-js-step-card-list]'));

    cardLists.forEach(function (list) {
      var cards = Array.from(list.querySelectorAll('[data-js-step-card]'));

      cards.forEach(function (card, index) {
        var nextCard = cards[index + 1];
        var continueAction = card.querySelector('[data-js-step-card-continue]');

        if (index > 0) {
          card.classList.add(CLASS_INACTIVE);
        }

        if (typeof nextCard !== 'undefined') {
          continueAction.addEventListener('click', function () {
            nextCard.classList.remove('is-inactive');
          });
        }
      });
    });
  };
});