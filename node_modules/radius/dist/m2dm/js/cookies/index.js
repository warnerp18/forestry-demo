(function (global, factory) {
  if (typeof define === "function" && define.amd) {
    define('radius/cookies/index', ['exports'], factory);
  } else if (typeof exports !== "undefined") {
    factory(exports);
  } else {
    var mod = {
      exports: {}
    };
    factory(mod.exports);
    global.radiusCookiesIndex = mod.exports;
  }
})(this, function (exports) {
  'use strict';

  Object.defineProperty(exports, "__esModule", {
    value: true
  });
  var cookies = {
    setCookie: function setCookie(_ref) {
      var _ref$cookieName = _ref.cookieName,
          cookieName = _ref$cookieName === undefined ? 'FCCookie' : _ref$cookieName,
          _ref$cookieValue = _ref.cookieValue,
          cookieValue = _ref$cookieValue === undefined ? true : _ref$cookieValue,
          _ref$cookieSecure = _ref.cookieSecure,
          cookieSecure = _ref$cookieSecure === undefined ? true : _ref$cookieSecure,
          _ref$cookieDays = _ref.cookieDays,
          cookieDays = _ref$cookieDays === undefined ? 365 : _ref$cookieDays,
          _ref$cookiePath = _ref.cookiePath,
          cookiePath = _ref$cookiePath === undefined ? '/' : _ref$cookiePath;

      var expires = cookies.getDaysFromNow(cookieDays);
      var secured = cookieSecure && window.location.protocol === 'https:' ? 'secure' : '';
      document.cookie = cookieName + '=' + cookieValue + ';      expires=' + expires + ';      path=' + cookiePath + ';' + secured;
    },
    getCookie: function getCookie(cookieName) {
      var name = cookieName + '=';
      var cookieArray = document.cookie.split(';');
      for (var i = 0; i < cookieArray.length; i += 1) {
        var cookie = cookieArray[i].trim();
        if (cookie.indexOf(name) === 0) {
          return cookie.substring(name.length, cookie.length);
        }
      }
      return '';
    },
    getDaysFromNow: function getDaysFromNow(days) {
      var date = new Date();
      date.setTime(date.getTime() + days * 24 * 60 * 60 * 1000);
      return date.toUTCString();
    }
  };

  exports['default'] = cookies;
});