import utils from './utils';

export default (jsClass, toggleClass, ctaSelector, targetSelector) => {
  const elements = `${ctaSelector}, ${targetSelector}`;

  utils.hasJS(jsClass, elements);
  utils.toggleClass(toggleClass, ctaSelector, 'click', targetSelector);
};
