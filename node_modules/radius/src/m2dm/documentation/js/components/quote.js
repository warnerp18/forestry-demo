export default () => {
  const htmlLangInput = document.querySelector('.html-lang-input');

  if (htmlLangInput) {
    htmlLangInput.addEventListener('change', (event) => {
      document.documentElement.lang = event.target.value;
    });
  }
};
