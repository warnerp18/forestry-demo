import React from 'react';
import { shallow } from 'enzyme';
import FeatureListItem from '../FeatureListItem';

describe('FeatureListItem', () => {
  let featureListItem;

  beforeEach(() => {
    featureListItem = shallow(<FeatureListItem>This is an item</FeatureListItem>);
  });

  it('renders the list item', () => {
    expect(featureListItem.html()).toBe('<li class="feature-list__item">This is an item</li>');
  });
});
